// IMPORTANT:
// - Write tests that lock in the desired behavior.
// - Then refactor implementation for readability.

describe('getHealthCheckResults', () => {
  it('TODO: calls api.get with expected URL + params and includes api key code when present', async () => {
    // This test should FAIL until you implement it properly.
    // Hints:
    // - jest.mock('./api', ...)
    // - set / unset globalThis.process.env.WEHEAT_FUNCTIONS_API_KEY
    // - import { getHealthCheckResults } from './getHealthCheckResults'
    throw new Error('TODO: write this test');
  });
});


